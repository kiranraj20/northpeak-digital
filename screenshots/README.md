# Lighthouse screenshots

Save the two Lighthouse report screenshots in **this folder** with these exact
filenames so the main README displays them on GitHub:

- `lighthouse-summary.png` — the report showing all category scores
  (Performance / Accessibility / Best Practices / SEO).
- `lighthouse-accessibility.png` — the Accessibility detail view (score 100).

PNG or JPG both work; just keep the names above (or update the paths in the
main `README.md` to match whatever you name them).
